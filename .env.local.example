# ========================================
# LoopArchitect Frontend Environment Config
# ========================================
# Copy this file to .env.local and fill in actual values.
# .env.local is in .gitignore and should NEVER be committed.

# Backend API origin (server-side only – used by the Next.js API proxy route)
# Local development:   http://localhost:8000
# Railway staging:     https://web-staging-cb7b.up.railway.app
# Railway production:  https://web-production-3afc5.up.railway.app
#
# REQUIRED in Vercel environment variables for every deployment environment.
# Without this, the proxy will return a 500 error instead of silently routing
# to the wrong backend.
BACKEND_ORIGIN=http://localhost:8000

# Backend origin for ALL browser-to-backend requests (GET, POST, uploads, etc.).
# Must be set at Vercel build time so Next.js can inline it into the browser bundle.
# ALL browser API calls go directly to this URL – no Next.js proxy is involved.
# Railway staging:     https://web-staging-cb7b.up.railway.app
# Railway production:  https://web-production-3afc5.up.railway.app
#
# REQUIRED in Vercel environment variables for every deployment environment.
# If this is not set, all browser API calls will throw an error at runtime.
NEXT_PUBLIC_BACKEND_ORIGIN=http://localhost:8000

# NOTE: NEXT_PUBLIC_API_URL is NOT used by this application.
# Do not set it – it is ignored and was removed to prevent accidental routing
# of browser API calls through the Vercel/Next.js proxy instead of directly
# to the Railway backend.
